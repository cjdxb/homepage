# 环境变量配置示例
# 复制此文件为 .env 并修改配置

# Flask 密钥 (必须修改！使用强随机字符串)
# 生成方法: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key

# Flask 环境
FLASK_ENV=production

# Gunicorn 配置 (可选)
GUNICORN_WORKERS=4
GUNICORN_BIND=0.0.0.0:5000
GUNICORN_LOG_LEVEL=info
